You can manage the options of the extension at device
level using:

- a [config.json](./config.json) file at the root of the
folder where the extension is installed
- a storage managed config like in [managed-storage.json](./managed-storage.json)
in your MDM software so to preload the config in the
extension storage and be used as option setup
